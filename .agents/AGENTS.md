# Project Rules for Werewolf Repository

- **STRICT GIT POLICY**:
  - NEVER execute `git commit` automatically.
  - NEVER execute `git push` automatically.
  - All code modifications must remain in the working directory as unstaged/staged edits until the user explicitly requests a commit or push.
